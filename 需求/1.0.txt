请帮我为这个程序创建打包所需要的文件，我只需要单文件，不要多余的目录文件，且打包后程序运行的时候，不要有任何的命令行窗口（包括ffmpeg的命令行处理窗口）

ffmpeg打包后需要用户放在与程序所在的同级目录下，而不是去环境变量中找，transnetv2-weights这个文件夹看看是否已经打包进入程序了，如果打包进入程序了，需要考虑打包后程序的运行问题，且请帮我设置一下程序图标，我放在项目根目录下为：ico.ico，打包的时候也请打包进去

我先将debug模式打开了，发现普通模式是正常的，但是智能场景分割却不正常，请你帮我解决下，debug信息我放在了 @debug.txt

请帮我将主程序中常用工具中这一页面给去掉，相关引用，代码，依赖和打包配置都去掉常用工具相关的